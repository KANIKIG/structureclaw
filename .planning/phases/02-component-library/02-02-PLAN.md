---
phase: 02-component-library
plan: 02
type: execute
wave: 3
depends_on: [02-01]
files_modified:
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/toast.tsx
  - frontend/src/app/providers.tsx
  - frontend/tests/components/dialog.test.tsx
  - frontend/tests/components/toast.test.tsx
autonomous: true
requirements: [COMP-06, COMP-07]
user_setup: []

must_haves:
  truths:
    - "Modal dialogs open/close smoothly and trap focus correctly"
    - "Toast notifications appear at bottom-right and auto-dismiss after 4 seconds"
    - "Toaster component is integrated into the provider tree"
  artifacts:
    - path: "frontend/src/components/ui/dialog.tsx"
      provides: "Dialog component with Radix UI primitive and focus trap"
      exports: ["Dialog", "DialogTrigger", "DialogContent", "DialogHeader", "DialogTitle", "DialogDescription", "DialogFooter"]
    - path: "frontend/src/components/ui/toast.tsx"
      provides: "Sonner Toaster wrapper with theme support"
      exports: ["Toaster"]
    - path: "frontend/src/app/providers.tsx"
      provides: "Root provider wrapping with Toaster"
      contains: "Toaster"
  key_links:
    - from: "components/ui/dialog.tsx"
      to: "@radix-ui/react-dialog"
      via: "DialogPrimitive"
      pattern: "import.*DialogPrimitive"
    - from: "components/ui/toast.tsx"
      to: "sonner"
      via: "Toaster import"
      pattern: "import.*Toaster.*from.*sonner"
    - from: "app/providers.tsx"
      to: "components/ui/toast.tsx"
      via: "Toaster component"
      pattern: "import.*Toaster"
---

<objective>
Build Dialog/Modal and Toast notification components with full accessibility support.

Purpose: Enable modal interactions and user feedback notifications
Output: Dialog component with focus trap, Toast system with Sonner integration
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library/02-RESEARCH.md
@.planning/phases/02-component-library/02-VALIDATION.md

<interfaces>
<!-- Existing patterns and dependencies -->
From frontend/src/components/ui/button.tsx:
```typescript
// Pattern: forwardRef, displayName, cn() usage
```

From frontend/src/components/theme-provider.tsx:
```typescript
import { useTheme } from "next-themes"
// Pattern: useTheme hook for theme-aware components
```

From frontend/src/app/globals.css:
```css
--background: 0 0% 100%;
--foreground: 222.2 84% 4.9%;
--border: 214.3 31.8% 91.4%;
/* Dark mode values in .dark class */
```

Required new dependencies:
- @radix-ui/react-dialog (for Dialog)
- sonner (for Toast)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Dialog and Toast dependencies</name>
  <files>frontend/package.json</files>
  <action>
Install required npm packages:
```bash
cd frontend && npm install @radix-ui/react-dialog sonner
```

Verify installation by checking package.json contains the new dependencies.
  </action>
  <verify>
    <automated>grep -E '"@radix-ui/react-dialog"|"sonner"' frontend/package.json</automated>
  </verify>
  <done>@radix-ui/react-dialog and sonner installed in package.json</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Dialog component (COMP-06)</name>
  <files>frontend/src/components/ui/dialog.tsx, frontend/tests/components/dialog.test.tsx</files>
  <behavior>
    - Test 1: Dialog renders trigger and content
    - Test 2: Dialog opens when trigger is clicked
    - Test 3: Dialog closes when Escape key is pressed
    - Test 4: DialogContent has proper role="dialog"
    - Test 5: DialogOverlay has proper overlay styling
    - Test 6: DialogTitle and DialogDescription render correctly
  </behavior>
  <action>
Create Dialog component wrapping Radix UI Dialog primitive:

```typescript
import * as DialogPrimitive from "@radix-ui/react-dialog"
import { X } from "lucide-react"
import { cn } from "@/lib/utils"
```

Export these components:
- Dialog = DialogPrimitive.Root
- DialogTrigger = DialogPrimitive.Trigger
- DialogPortal = DialogPrimitive.Portal
- DialogClose = DialogPrimitive.Close
- DialogOverlay: fixed inset-0 z-50 bg-black/80 with fade animation
- DialogContent: fixed centered z-50 grid w-full max-w-lg with zoom animation, includes X close button
- DialogHeader: flex flex-col space-y-1.5 text-center sm:text-left
- DialogTitle: text-lg font-semibold leading-none tracking-tight
- DialogDescription: text-sm text-muted-foreground
- DialogFooter: flex flex-col-reverse sm:flex-row sm:justify-end sm:space-x-2

Use data-[state=open/closed]:animate-in/out with fade and zoom effects.
Set displayName for each forwardRef component.

Replace it.todo() with actual tests, run tests to confirm RED then GREEN.
  </action>
  <verify>
    <automated>npm test -- dialog.test.tsx --run</automated>
  </verify>
  <done>Dialog component with all subcomponents, all 6 tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Toast component (COMP-07)</name>
  <files>frontend/src/components/ui/toast.tsx, frontend/tests/components/toast.test.tsx</files>
  <behavior>
    - Test 1: Toaster component renders without errors
    - Test 2: Toaster uses theme from ThemeProvider
    - Test 3: Toaster has position="bottom-right" by default
    - Test 4: toast() function is importable from sonner
  </behavior>
  <action>
Create Toast component wrapping Sonner:

```typescript
"use client"

import { useTheme } from "next-themes"
import { Toaster as Sonner } from "sonner"
import type { ToasterProps } from "sonner"
```

Toaster component:
- Uses useTheme() hook to get current theme
- Passes theme to Sonner's theme prop
- Sets position="bottom-right" (per ROADMAP requirement)
- Sets richColors={true} for colored icons
- Applies toast classNames for styling consistency with design system:
  - toast: bg-background text-foreground border-border shadow-lg
  - description: text-muted-foreground
  - actionButton: bg-primary text-primary-foreground
  - cancelButton: bg-muted text-muted-foreground

Also export toast function from sonner for convenience:
```typescript
export { toast } from "sonner"
```

Replace it.todo() with actual tests, run tests to confirm RED then GREEN.
  </action>
  <verify>
    <automated>npm test -- toast.test.tsx --run</automated>
  </verify>
  <done>Toaster component with Sonner integration, all 4 tests pass</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Toaster into providers</name>
  <files>frontend/src/app/providers.tsx</files>
  <action>
Create or update providers.tsx to include Toaster:

If providers.tsx does not exist, create it:
```typescript
"use client"

import { ThemeProvider } from "@/components/theme-provider"
import { Toaster } from "@/components/ui/toast"

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <ThemeProvider
      attribute="class"
      defaultTheme="system"
      enableSystem
      disableTransitionOnChange
    >
      {children}
      <Toaster />
    </ThemeProvider>
  )
}
```

If providers.tsx exists, add Toaster import and <Toaster /> after {children}.

Verify the Toaster is positioned after children to ensure proper z-index stacking.
  </action>
  <verify>
    <automated>grep -E 'Toaster|toast' frontend/src/app/providers.tsx</automated>
  </verify>
  <done>Providers.tsx includes Toaster component in the provider tree</done>
</task>

</tasks>

<verification>
- Dialog component exists with all subcomponents
- Toast/Toaster component exists with Sonner integration
- Toaster is added to providers.tsx
- All tests pass
- Components use CSS variables for theming
</verification>

<success_criteria>
- Dialog opens/closes with animation and focus trap
- Toast notifications appear at bottom-right
- Toaster integrated into app providers
- All 10+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-library/02-02-SUMMARY.md`
</output>
