---
phase: 02-component-library
plan: 03
type: execute
wave: 3
depends_on: [02-01]
files_modified:
  - frontend/src/components/ui/button.tsx
  - frontend/tests/components/button.test.tsx
  - frontend/tests/components/card.test.tsx
autonomous: true
requirements: [COMP-01, COMP-02]
user_setup: []

must_haves:
  truths:
    - "Developer can use Button with multiple sizes (sm, md, lg) and variants (default, destructive, outline, ghost)"
    - "Developer can use Card with subcomponents (Header, Title, Description, Content, Footer)"
    - "Button and Card components have test coverage"
  artifacts:
    - path: "frontend/src/components/ui/button.tsx"
      provides: "Button component with cva variants"
      exports: ["Button", "buttonVariants"]
      min_lines: 50
    - path: "frontend/src/components/ui/card.tsx"
      provides: "Card container with subcomponents"
      exports: ["Card", "CardHeader", "CardTitle", "CardDescription", "CardContent", "CardFooter"]
      min_lines: 70
  key_links:
    - from: "components/ui/button.tsx"
      to: "lib/utils.ts"
      via: "cn() and cva"
      pattern: "import.*cva.*from.*class-variance-authority"
    - from: "components/ui/card.tsx"
      to: "lib/utils.ts"
      via: "cn() utility"
      pattern: "import.*cn.*from.*utils"
---

<objective>
Verify and enhance existing Button and Card components with complete test coverage.

Purpose: Ensure foundational components have full TDD test coverage and follow established patterns
Output: Enhanced Button with asChild support via @radix-ui/react-slot, Card tests, both with full coverage
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library/02-RESEARCH.md
@.planning/phases/02-component-library/02-VALIDATION.md

<interfaces>
<!-- Existing Button and Card implementations -->
From frontend/src/components/ui/button.tsx:
```typescript
// Already has cva variants: default, destructive, outline, secondary, ghost, link
// Already has sizes: default, sm, lg, icon
// Already has ButtonProps with asChild but not implemented
```

From frontend/src/components/ui/card.tsx:
```typescript
// Already has: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
// All using simple forwardRef pattern
```

From frontend/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

Required for asChild support:
- @radix-ui/react-slot (allows polymorphic Button rendering)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Radix slot for asChild support</name>
  <files>frontend/package.json</files>
  <action>
Install @radix-ui/react-slot for Button's asChild polymorphic pattern:
```bash
cd frontend && npm install @radix-ui/react-slot
```

This enables Button to render as a different element (e.g., Next.js Link) while keeping button styling.
  </action>
  <verify>
    <automated>grep '"@radix-ui/react-slot"' frontend/package.json</automated>
  </verify>
  <done>@radix-ui/react-slot installed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enhance Button with asChild support (COMP-01)</name>
  <files>frontend/src/components/ui/button.tsx, frontend/tests/components/button.test.tsx</files>
  <behavior>
    - Test 1: Renders button element by default
    - Test 2: Renders with default variant (bg-primary)
    - Test 3: Renders with destructive variant (bg-destructive)
    - Test 4: Renders with outline variant (border, transparent bg)
    - Test 5: Renders with ghost variant (no border, hover only)
    - Test 6: Renders with sm, md (default), lg sizes
    - Test 7: Has focus-visible:ring-2 ring-ring for accessibility
    - Test 8: Is disabled when disabled prop is true
    - Test 9: Renders as Slot child when asChild=true
  </behavior>
  <action>
Enhance existing Button component to support asChild pattern:

```typescript
import { Slot } from "@radix-ui/react-slot"

// In Button component:
const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, asChild = false, ...props }, ref) => {
    const Comp = asChild ? Slot : "button"
    return (
      <Comp
        className={cn(buttonVariants({ variant, size, className }))}
        ref={ref}
        {...props}
      />
    )
  }
)
```

The existing Button already has the interface with asChild prop - just need to implement the logic.

Replace it.todo() in button.test.tsx with actual tests.
Run tests to confirm RED then GREEN.
  </action>
  <verify>
    <automated>npm test -- button.test.tsx --run</automated>
  </verify>
  <done>Button has asChild support via Slot, all 9 tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Card component tests (COMP-02)</name>
  <files>frontend/tests/components/card.test.tsx</files>
  <behavior>
    - Test 1: Card renders children inside div
    - Test 2: Card has rounded-lg border bg-card shadow-sm classes
    - Test 3: CardHeader has flex flex-col space-y-1.5 p-6
    - Test 4: CardTitle renders h3 with text-2xl font-semibold
    - Test 5: CardDescription renders p with text-muted-foreground
    - Test 6: CardContent has p-6 pt-0 padding
    - Test 7: CardFooter has flex items-center p-6 pt-0
    - Test 8: All components forward ref correctly
  </behavior>
  <action>
Card component already exists - just add tests.

Replace it.todo() in card.test.tsx with actual tests for:
- Card: renders children, has correct base classes
- CardHeader: renders with spacing
- CardTitle: renders as h3 with correct typography
- CardDescription: renders with muted foreground color
- CardContent: renders with padding
- CardFooter: renders as flex container
- All components forward refs

Run tests to confirm GREEN (Card already implemented).
  </action>
  <verify>
    <automated>npm test -- card.test.tsx --run</automated>
  </verify>
  <done>Card tests complete, all 8 tests pass</done>
</task>

</tasks>

<verification>
- Button has asChild support with @radix-ui/react-slot
- Button tests cover all variants and sizes
- Card tests cover all subcomponents
- All tests pass
- Components use CSS variables for theming
</verification>

<success_criteria>
- Button works with asChild for polymorphic rendering
- Button has 9 passing tests
- Card has 8 passing tests
- npm test -- --run passes for button and card tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-library/02-03-SUMMARY.md`
</output>
