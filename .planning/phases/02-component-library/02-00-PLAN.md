---
phase: 02-component-library
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/tests/components/button.test.tsx
  - frontend/tests/components/input.test.tsx
  - frontend/tests/components/textarea.test.tsx
  - frontend/tests/components/select.test.tsx
  - frontend/tests/components/card.test.tsx
  - frontend/tests/components/dialog.test.tsx
  - frontend/tests/components/toast.test.tsx
  - frontend/tests/components/skeleton.test.tsx
  - frontend/tests/components/badge.test.tsx
  - frontend/tests/components/command.test.tsx
  - frontend/tests/lib/animations.test.ts
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, COMP-07, COMP-08, COMP-09, COMP-10, COMP-11]
user_setup: []

must_haves:
  truths:
    - "All 11 component requirements have corresponding test stub files"
    - "Each test file contains it.todo() stubs for expected behaviors"
    - "Tests are discoverable by vitest via tests/components/ and tests/lib/ paths"
  artifacts:
    - path: "frontend/tests/components/button.test.tsx"
      provides: "Test stubs for Button variants (COMP-01)"
      contains: "it.todo"
    - path: "frontend/tests/components/input.test.tsx"
      provides: "Test stubs for Input focus states (COMP-03)"
      contains: "it.todo"
    - path: "frontend/tests/components/textarea.test.tsx"
      provides: "Test stubs for Textarea (COMP-04)"
      contains: "it.todo"
    - path: "frontend/tests/components/select.test.tsx"
      provides: "Test stubs for Select dropdown (COMP-05)"
      contains: "it.todo"
    - path: "frontend/tests/components/card.test.tsx"
      provides: "Test stubs for Card subcomponents (COMP-02)"
      contains: "it.todo"
    - path: "frontend/tests/components/dialog.test.tsx"
      provides: "Test stubs for Dialog focus trap (COMP-06)"
      contains: "it.todo"
    - path: "frontend/tests/components/toast.test.tsx"
      provides: "Test stubs for Toast notifications (COMP-07)"
      contains: "it.todo"
    - path: "frontend/tests/components/skeleton.test.tsx"
      provides: "Test stubs for Skeleton loading (COMP-08)"
      contains: "it.todo"
    - path: "frontend/tests/components/badge.test.tsx"
      provides: "Test stubs for Badge variants (COMP-09)"
      contains: "it.todo"
    - path: "frontend/tests/components/command.test.tsx"
      provides: "Test stubs for Command palette (COMP-10)"
      contains: "it.todo"
    - path: "frontend/tests/lib/animations.test.ts"
      provides: "Test stubs for micro-interactions (COMP-11)"
      contains: "it.todo"
  key_links:
    - from: "vitest.config.ts"
      to: "tests/**/*.test.ts*"
      via: "include pattern"
      pattern: "include.*tests"
---

<objective>
Create test stub files for all Phase 2 component requirements, enabling TDD workflow for subsequent plans.

Purpose: Establish test scaffolding before implementation so each plan can follow RED-GREEN-REFACTOR
Output: 11 test stub files with it.todo() placeholders for expected behaviors
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library/02-VALIDATION.md

<interfaces>
<!-- Existing patterns from Phase 1 -->
From frontend/tests/cn-utility.test.ts:
```typescript
import { describe, it, expect } from 'vitest'
// Pattern: describe('Requirement Name (ID)', () => { it.todo('behavior') })
```

From frontend/tests/theme-switch.test.tsx:
```typescript
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
// Pattern: it.todo() for stubs to be implemented during feature development
```

From frontend/vitest.config.ts:
```typescript
include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx']
// Tests must be in tests/ directory
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs directory structure</name>
  <files>frontend/tests/components/.gitkeep, frontend/tests/lib/.gitkeep</files>
  <action>Create directories for component and lib tests. Add .gitkeep files to ensure empty directories are tracked by git.</action>
  <verify>
    <automated>test -d frontend/tests/components && test -d frontend/tests/lib</automated>
  </verify>
  <done>Test directories created and tracked in git</done>
</task>

<task type="auto">
  <name>Task 2: Create Button test stubs (COMP-01)</name>
  <files>frontend/tests/components/button.test.tsx</files>
  <action>
Create test file with it.todo() stubs for:
- Renders with default variant
- Renders with destructive variant
- Renders with outline variant
- Renders with ghost variant
- Renders with sm, md, lg sizes
- Has proper focus ring on focus-visible
- Is disabled when disabled prop is true

Use pattern: describe('Button Component (COMP-01)', () => { it.todo('...') })
  </action>
  <verify>
    <automated>npm test -- button.test.tsx --run</automated>
  </verify>
  <done>Button test stubs exist with 7+ it.todo() entries, tests run without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create Input test stubs (COMP-03)</name>
  <files>frontend/tests/components/input.test.tsx</files>
  <action>
Create test file with it.todo() stubs for:
- Renders input element
- Has focus-visible ring state
- Applies disabled styling
- Accepts placeholder text
- Uses CSS variable colors for theming

Use pattern: describe('Input Component (COMP-03)', () => { it.todo('...') })
  </action>
  <verify>
    <automated>npm test -- input.test.tsx --run</automated>
  </verify>
  <done>Input test stubs exist with 5+ it.todo() entries</done>
</task>

<task type="auto">
  <name>Task 4: Create Textarea test stubs (COMP-04)</name>
  <files>frontend/tests/components/textarea.test.tsx</files>
  <action>
Create test file with it.todo() stubs for:
- Renders textarea element
- Has consistent focus states with Input
- Supports multi-line input
- Applies disabled styling

Use pattern: describe('Textarea Component (COMP-04)', () => { it.todo('...') })
  </action>
  <verify>
    <automated>npm test -- textarea.test.tsx --run</automated>
  </verify>
  <done>Textarea test stubs exist with 4+ it.todo() entries</done>
</task>

<task type="auto">
  <name>Task 5: Create Select test stubs (COMP-05)</name>
  <files>frontend/tests/components/select.test.tsx</files>
  <action>
Create test file with it.todo() stubs for:
- Renders trigger with placeholder
- Opens dropdown on click
- Shows options when open
- Selects value on option click
- Supports keyboard navigation (ArrowUp, ArrowDown, Enter)
- Closes on Escape key
- Applies focus ring styling

Use pattern: describe('Select Component (COMP-05)', () => { it.todo('...') })
  </action>
  <verify>
    <automated>npm test -- select.test.tsx --run</automated>
  </verify>
  <done>Select test stubs exist with 7+ it.todo() entries</done>
</task>

<task type="auto">
  <name>Task 6: Create Card test stubs (COMP-02)</name>
  <files>frontend/tests/components/card.test.tsx</files>
  <action>
Create test file with it.todo() stubs for:
- Card renders children
- CardHeader renders with proper spacing
- CardTitle renders heading
- CardDescription renders muted text
- CardContent renders with padding
- CardFooter renders flex container
- Uses CSS variable colors

Use pattern: describe('Card Component (COMP-02)', () => { it.todo('...') })
  </action>
  <verify>
    <automated>npm test -- card.test.tsx --run</automated>
  </verify>
  <done>Card test stubs exist with 7+ it.todo() entries</done>
</task>

<task type="auto">
  <name>Task 7: Create Dialog test stubs (COMP-06)</name>
  <files>frontend/tests/components/dialog.test.tsx</files>
  <action>
Create test file with it.todo() stubs for:
- Dialog opens when trigger is clicked
- Dialog closes when close button is clicked
- Dialog closes on Escape key
- Focus is trapped within dialog (Tab cycles inside)
- Focus returns to trigger when dialog closes
- Overlay prevents interaction with background
- Uses Radix UI Dialog primitive

Use pattern: describe('Dialog Component (COMP-06)', () => { it.todo('...') })
  </action>
  <verify>
    <automated>npm test -- dialog.test.tsx --run</automated>
  </verify>
  <done>Dialog test stubs exist with 7+ it.todo() entries</done>
</task>

<task type="auto">
  <name>Task 8: Create Toast test stubs (COMP-07)</name>
  <files>frontend/tests/components/toast.test.tsx</files>
  <action>
Create test file with it.todo() stubs for:
- Toast appears when toast() is called
- Toast appears at bottom-right position
- Toast auto-dismisses after 4 seconds
- Toast supports success, error, info types
- Multiple toasts stack properly
- Uses Sonner library

Use pattern: describe('Toast Component (COMP-07)', () => { it.todo('...') })
  </action>
  <verify>
    <automated>npm test -- toast.test.tsx --run</automated>
  </verify>
  <done>Toast test stubs exist with 6+ it.todo() entries</done>
</task>

<task type="auto">
  <name>Task 9: Create Skeleton test stubs (COMP-08)</name>
  <files>frontend/tests/components/skeleton.test.tsx</files>
  <action>
Create test file with it.todo() stubs for:
- Skeleton renders with pulse animation
- Skeleton accepts custom className
- Skeleton uses muted background color
- Skeleton has rounded corners

Use pattern: describe('Skeleton Component (COMP-08)', () => { it.todo('...') })
  </action>
  <verify>
    <automated>npm test -- skeleton.test.tsx --run</automated>
  </verify>
  <done>Skeleton test stubs exist with 4+ it.todo() entries</done>
</task>

<task type="auto">
  <name>Task 10: Create Badge test stubs (COMP-09)</name>
  <files>frontend/tests/components/badge.test.tsx</files>
  <action>
Create test file with it.todo() stubs for:
- Badge renders with default variant
- Badge renders with secondary variant
- Badge renders with destructive variant
- Badge renders with outline variant
- Badge has rounded-full shape
- Badge uses cva for variants

Use pattern: describe('Badge Component (COMP-09)', () => { it.todo('...') })
  </action>
  <verify>
    <automated>npm test -- badge.test.tsx --run</automated>
  </verify>
  <done>Badge test stubs exist with 6+ it.todo() entries</done>
</task>

<task type="auto">
  <name>Task 11: Create Command palette test stubs (COMP-10)</name>
  <files>frontend/tests/components/command.test.tsx</files>
  <action>
Create test file with it.todo() stubs for:
- Command palette opens with Cmd/Ctrl+K
- Command palette closes on Escape
- Command palette provides fuzzy search
- Command items are keyboard navigable
- Uses cmdk library

Use pattern: describe('Command Palette (COMP-10)', () => { it.todo('...') })
  </action>
  <verify>
    <automated>npm test -- command.test.tsx --run</automated>
  </verify>
  <done>Command test stubs exist with 5+ it.todo() entries</done>
</task>

<task type="auto">
  <name>Task 12: Create animations test stubs (COMP-11)</name>
  <files>frontend/tests/lib/animations.test.ts</files>
  <action>
Create test file with it.todo() stubs for:
- Hover transitions use 150ms duration
- Click feedback uses scale transform
- Focus ring is instant (0ms)
- Modal animations use 200ms duration
- Toast enter/exit animations work correctly

Use pattern: describe('Micro-interactions (COMP-11)', () => { it.todo('...') })
  </action>
  <verify>
    <automated>npm test -- animations.test.ts --run</automated>
  </verify>
  <done>Animations test stubs exist with 5+ it.todo() entries</done>
</task>

</tasks>

<verification>
- All 11 test stub files created
- All tests run without errors (it.todo() passes)
- vitest.config.ts includes tests/**/*.test.ts* pattern
</verification>

<success_criteria>
- 11 test stub files exist in tests/components/ and tests/lib/
- Each file has describe block with component name and requirement ID
- Each file has 4-7 it.todo() entries for expected behaviors
- npm test -- --run passes with all stubs
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-library/02-00-SUMMARY.md`
</output>
