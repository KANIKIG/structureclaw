---
phase: 02-component-library
plan: 01
type: execute
wave: 2
depends_on: [02-00]
files_modified:
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/textarea.tsx
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/tests/components/input.test.tsx
  - frontend/tests/components/textarea.test.tsx
  - frontend/tests/components/select.test.tsx
  - frontend/tests/components/badge.test.tsx
  - frontend/tests/components/skeleton.test.tsx
autonomous: true
requirements: [COMP-03, COMP-04, COMP-05, COMP-08, COMP-09]
user_setup: []

must_haves:
  truths:
    - "Developer can use Input with consistent focus states and styling"
    - "Developer can use Textarea for multi-line input with same styling as Input"
    - "Developer can use Select for dropdown selection with keyboard navigation"
    - "Developer can use Badge with multiple variants (default, secondary, destructive, outline)"
    - "Developer can use Skeleton for loading placeholder states"
  artifacts:
    - path: "frontend/src/components/ui/input.tsx"
      provides: "Input component with focus ring styling"
      exports: ["Input"]
    - path: "frontend/src/components/ui/textarea.tsx"
      provides: "Textarea component matching Input styling"
      exports: ["Textarea"]
    - path: "frontend/src/components/ui/select.tsx"
      provides: "Select dropdown with Radix UI primitive"
      exports: ["Select", "SelectTrigger", "SelectValue", "SelectContent", "SelectItem"]
    - path: "frontend/src/components/ui/badge.tsx"
      provides: "Badge component with cva variants"
      exports: ["Badge", "badgeVariants"]
    - path: "frontend/src/components/ui/skeleton.tsx"
      provides: "Skeleton loading component"
      exports: ["Skeleton"]
  key_links:
    - from: "components/ui/input.tsx"
      to: "lib/utils.ts"
      via: "cn() utility"
      pattern: "import.*cn.*from.*utils"
    - from: "components/ui/select.tsx"
      to: "@radix-ui/react-select"
      via: "SelectPrimitive"
      pattern: "import.*SelectPrimitive"
    - from: "components/ui/badge.tsx"
      to: "class-variance-authority"
      via: "cva function"
      pattern: "import.*cva"
---

<objective>
Build form input components (Input, Textarea, Select) and utility components (Badge, Skeleton) following shadcn/ui patterns.

Purpose: Establish consistent form controls with focus states and loading indicators
Output: 5 new UI components with full test coverage
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library/02-RESEARCH.md
@.planning/phases/02-component-library/02-VALIDATION.md

<interfaces>
<!-- Existing patterns from Phase 1 and existing components -->
From frontend/src/components/ui/button.tsx:
```typescript
import { cva, type VariantProps } from "class-variance-authority"
import { cn } from "@/lib/utils"
// Pattern: cva for variants, forwardRef, displayName
```

From frontend/src/components/ui/card.tsx:
```typescript
import * as React from "react"
import { cn } from "@/lib/utils"
// Pattern: simple forwardRef with cn() for class composition
```

From frontend/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
// Use for all class merging
```

From frontend/src/app/globals.css:
```css
/* Use these CSS variables for theming */
--input: 214.3 31.8% 91.4%;
--ring: 24 95% 53%;
--muted: 210 40% 96.1%;
--primary: 24 95% 53%;
--destructive: 0 84.2% 60.2%;
--radius: 0.5rem;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Input component (COMP-03)</name>
  <files>frontend/src/components/ui/input.tsx, frontend/tests/components/input.test.tsx</files>
  <behavior>
    - Test 1: Renders input element with default styling
    - Test 2: Has focus-visible ring state using ring-ring color
    - Test 3: Applies disabled:cursor-not-allowed and disabled:opacity-50 when disabled
    - Test 4: Accepts custom className prop
    - Test 5: Forwards ref to input element
  </behavior>
  <action>
Create Input component following shadcn/ui pattern:
- forwardRef to HTMLInputElement
- Extends React.InputHTMLAttributes<HTMLInputElement>
- Base classes: flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm
- Focus: focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2
- Disabled: disabled:cursor-not-allowed disabled:opacity-50
- Placeholder: placeholder:text-muted-foreground
- Use cn() for class composition
- Set displayName = "Input"

Replace it.todo() with actual tests, run tests to confirm RED then GREEN.
  </action>
  <verify>
    <automated>npm test -- input.test.tsx --run</automated>
  </verify>
  <done>Input component renders, has focus ring, supports disabled state, all 5 tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Textarea component (COMP-04)</name>
  <files>frontend/src/components/ui/textarea.tsx, frontend/tests/components/textarea.test.tsx</files>
  <behavior>
    - Test 1: Renders textarea element with consistent Input styling
    - Test 2: Has same focus-visible ring state as Input
    - Test 3: Applies disabled styling when disabled
    - Test 4: Supports min-height for multi-line input
  </behavior>
  <action>
Create Textarea component matching Input pattern:
- forwardRef to HTMLTextAreaElement
- Extends React.TextareaHTMLAttributes<HTMLTextAreaElement>
- Same base classes as Input but without h-10 (use min-h-[80px] for multi-line)
- Same focus ring pattern as Input
- Same disabled styling as Input
- Use cn() for class composition
- Set displayName = "Textarea"

Replace it.todo() with actual tests, run tests to confirm RED then GREEN.
  </action>
  <verify>
    <automated>npm test -- textarea.test.tsx --run</automated>
  </verify>
  <done>Textarea component matches Input styling, all 4 tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Select component (COMP-05)</name>
  <files>frontend/src/components/ui/select.tsx, frontend/tests/components/select.test.tsx</files>
  <behavior>
    - Test 1: SelectTrigger renders with consistent Input styling
    - Test 2: SelectTrigger shows ChevronDown icon
    - Test 3: SelectContent renders items when open
    - Test 4: SelectItem shows check icon when selected
    - Test 5: Keyboard navigation works (ArrowUp, ArrowDown, Enter, Escape)
  </behavior>
  <action>
First, install required dependencies:
```bash
cd frontend && npm install @radix-ui/react-select lucide-react
```

Create Select component with Radix UI primitive:
- Export Select (Root), SelectTrigger, SelectValue, SelectContent, SelectItem, SelectGroup
- SelectTrigger: forwardRef, h-10 w-full rounded-md border border-input bg-background px-3 py-2
- Include ChevronDown icon from lucide-react
- SelectContent: rounded-md border bg-popover p-1 shadow-md
- SelectItem: relative flex w-full select-none items-center rounded-sm py-1.5 pl-8 pr-2 text-sm
- Include Check icon for selected state
- Use cn() for all class composition
- Set displayName for each component

Replace it.todo() with actual tests, run tests to confirm RED then GREEN.
  </action>
  <verify>
    <automated>npm test -- select.test.tsx --run</automated>
  </verify>
  <done>Select component with trigger, content, and items, all 5 tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement Badge component (COMP-09)</name>
  <files>frontend/src/components/ui/badge.tsx, frontend/tests/components/badge.test.tsx</files>
  <behavior>
    - Test 1: Renders with default variant (bg-primary)
    - Test 2: Renders with secondary variant (bg-secondary)
    - Test 3: Renders with destructive variant (bg-destructive)
    - Test 4: Renders with outline variant (border only)
    - Test 5: Has rounded-full shape
    - Test 6: Accepts custom className
  </behavior>
  <action>
Create Badge component using cva for variants:
- Use cva with variants: default, secondary, destructive, outline
- Base classes: inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold
- Variants:
  - default: border-transparent bg-primary text-primary-foreground
  - secondary: border-transparent bg-secondary text-secondary-foreground
  - destructive: border-transparent bg-destructive text-destructive-foreground
  - outline: text-foreground (no background)
- Export BadgeProps interface extending HTMLAttributes and VariantProps
- Export badgeVariants for reuse
- Set displayName = "Badge"

Replace it.todo() with actual tests, run tests to confirm RED then GREEN.
  </action>
  <verify>
    <automated>npm test -- badge.test.tsx --run</automated>
  </verify>
  <done>Badge component with 4 variants, all 6 tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement Skeleton component (COMP-08)</name>
  <files>frontend/src/components/ui/skeleton.tsx, frontend/tests/components/skeleton.test.tsx</files>
  <behavior>
    - Test 1: Renders with animate-pulse class
    - Test 2: Uses bg-muted for color
    - Test 3: Has rounded-md by default
    - Test 4: Accepts custom className
  </behavior>
  <action>
Create Skeleton component (simple, no variants needed):
- Simple forwardRef to HTMLDivElement
- Base classes: animate-pulse rounded-md bg-muted
- Extends React.HTMLAttributes<HTMLDivElement>
- Use cn() for class composition
- Set displayName = "Skeleton"

Replace it.todo() with actual tests, run tests to confirm RED then GREEN.
  </action>
  <verify>
    <automated>npm test -- skeleton.test.tsx --run</automated>
  </verify>
  <done>Skeleton component with pulse animation, all 4 tests pass</done>
</task>

</tasks>

<verification>
- All 5 components exist in src/components/ui/
- All components export correctly named exports
- All tests pass (replace it.todo with real tests)
- Components use CSS variables for theming (no hardcoded colors)
- All components use cn() for class composition
</verification>

<success_criteria>
- Input, Textarea, Select, Badge, Skeleton components implemented
- All components follow shadcn/ui patterns
- All tests pass (20+ tests total)
- npm test -- --run passes for all 5 test files
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-library/02-01-SUMMARY.md`
</output>
