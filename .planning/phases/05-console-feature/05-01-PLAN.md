---
phase: 05-console-feature
plan: 01
type: execute
wave: 1
depends_on: ["05-00"]
files_modified:
  - frontend/src/lib/stores/slices/console.ts
  - frontend/src/lib/api/contracts/agent.ts
  - frontend/src/components/console/endpoint-selector.tsx
  - frontend/src/components/console/message-input.tsx
  - frontend/src/components/console/model-json-panel.tsx
  - frontend/src/components/console/index.ts
  - frontend/tests/components/console/endpoint-selector.test.tsx
  - frontend/tests/components/console/message-input.test.tsx
  - frontend/tests/components/console/model-json-panel.test.tsx
  - frontend/tests/stores/slices/console.test.ts
  - frontend/tests/api/contracts/agent.test.ts
autonomous: true
requirements:
  - CONS-01
  - CONS-02
  - CONS-03
  - CONS-04
user_setup: []
must_haves:
  truths:
    - "User can select endpoint (agent-run, chat-message, chat-execute)"
    - "User can select mode (chat, execute, auto)"
    - "Mode selector is disabled when chat-execute endpoint is selected"
    - "User can input message text"
    - "User can optionally expand JSON model input area"
    - "Model JSON panel validates JSON before submission"
  artifacts:
    - path: "frontend/src/components/console/endpoint-selector.tsx"
      provides: "Endpoint and mode selection UI"
      exports: ["EndpointSelector"]
    - path: "frontend/src/components/console/message-input.tsx"
      provides: "Message text input"
      exports: ["MessageInput"]
    - path: "frontend/src/components/console/model-json-panel.tsx"
      provides: "Collapsible model JSON input"
      exports: ["ModelJsonPanel"]
    - path: "frontend/src/lib/stores/slices/console.ts"
      provides: "Extended console state with form inputs"
    - path: "frontend/src/lib/api/contracts/agent.ts"
      provides: "Type definitions for API payloads"
  key_links:
    - from: "frontend/src/components/console/endpoint-selector.tsx"
      to: "frontend/src/lib/stores/slices/console.ts"
      via: "useStore selector"
      pattern: "useStore\\(.*state\\.endpoint"
    - from: "frontend/src/components/console/model-json-panel.tsx"
      to: "frontend/src/components/ui/collapsible.tsx"
      via: "Collapsible import"
      pattern: "from '@/components/ui/collapsible'"
---

<objective>
Build console input controls for endpoint, mode, message, and model JSON input.

Purpose: Provide the input layer for console feature where users configure their requests.
Output: EndpointSelector, MessageInput, ModelJsonPanel components + extended console slice + API contracts
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-console-feature/05-RESEARCH.md
@.planning/phases/05-console-feature/05-VALIDATION.md

## Existing Console Slice (to extend)

From frontend/src/lib/stores/slices/console.ts:
```typescript
export interface ConsoleState {
  endpoint: 'agent-run' | 'chat-message' | 'chat-execute'
  mode: 'chat' | 'execute' | 'auto'
  conversationId: string | null
  traceId: string | null
}

export interface ConsoleActions {
  setEndpoint: (endpoint: ConsoleState['endpoint']) => void
  setMode: (mode: ConsoleState['mode']) => void
  setConversationId: (id: string | null) => void
  resetConsole: () => void
}
```

## Existing UI Components

From frontend/src/components/ui/select.tsx:
- Select, SelectTrigger, SelectValue, SelectContent, SelectItem

From frontend/src/components/ui/textarea.tsx:
- Textarea with standard styling

From frontend/src/components/ui/input.tsx:
- Input with standard styling

From frontend/src/components/ui/checkbox.tsx (Wave 0):
- Checkbox component

From frontend/src/components/ui/collapsible.tsx (Wave 0):
- Collapsible, CollapsibleTrigger, CollapsibleContent

## Store Access Pattern

From frontend/src/lib/stores/context.tsx:
```typescript
export const useStore = <T,>(selector: (store: StoreState) => T): T => {
  const appStoreContext = useContext(AppStoreContext)
  if (!appStoreContext) {
    throw new Error('useStore must be used within AppStoreProvider')
  }
  return useZustandStore(appStoreContext, selector)
}
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend console slice with form state and API contracts</name>
  <files>
    frontend/src/lib/stores/slices/console.ts
    frontend/src/lib/api/contracts/agent.ts
    frontend/tests/stores/slices/console.test.ts
    frontend/tests/api/contracts/agent.test.ts
  </files>
  <behavior>
    - Console slice includes message, modelText, includeModel, analysisType, reportFormat, reportOutput
    - Console slice includes checkbox flags: autoAnalyze, autoCodeCheck, includeReport
    - Console slice includes execution state: loading, isStreaming, connectionState, result, error
    - API contracts define ChatMessageRequest, ChatExecuteRequest, AgentRunRequest types
    - Tests verify all new state fields and actions
  </behavior>
  <action>
    Extend the existing console slice to include all form state and execution state needed for the console feature. Create API contract types for request payloads.

    1. Update `frontend/src/lib/stores/slices/console.ts`:
       - Add types: AnalysisType, ReportFormat, ReportOutput, AgentMetrics, AgentToolCall, Clarification, AgentResult
       - Add ConsoleFormState interface with: message, modelText, includeModel, analysisType, reportFormat, reportOutput, autoAnalyze, autoCodeCheck, includeReport
       - Add ConsoleExecutionState interface with: loading, isStreaming, connectionState, result, rawResponse, streamFrames, error
       - Extend ConsoleState to include form and execution state
       - Add actions: setMessage, setModelText, setIncludeModel, setAnalysisType, setReportFormat, setReportOutput, setAutoAnalyze, setAutoCodeCheck, setIncludeReport, setLoading, setConnectionState, setResult, setRawResponse, setStreamFrames, setError
       - Update initialConsoleState with all new default values

    2. Create `frontend/src/lib/api/contracts/agent.ts`:
       - Define ChatMessageRequest, ChatExecuteRequest, AgentRunRequest interfaces
       - Define ContextPayload interface for shared context
       - Export all types

    3. Update `frontend/tests/stores/slices/console.test.ts`:
       - Add tests for all new state fields
       - Add tests for all new actions

    4. Create `frontend/tests/api/contracts/agent.test.ts`:
       - Add type existence tests (compile-time validation)
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/stores/slices/console.test.ts tests/api/contracts/agent.test.ts</automated>
  </verify>
  <done>Extended console slice with form and execution state, API contract types defined, all tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create EndpointSelector component</name>
  <files>
    frontend/src/components/console/endpoint-selector.tsx
    frontend/tests/components/console/endpoint-selector.test.tsx
  </files>
  <behavior>
    - EndpointSelector renders two Select components side by side (endpoint and mode)
    - Endpoint select has options: chat-message, chat-execute, agent-run
    - Mode select has options: chat, execute, auto
    - Mode select is disabled when endpoint is 'chat-execute'
    - Selecting an endpoint updates the Zustand store
    - Selecting a mode updates the Zustand store
  </behavior>
  <action>
    Create the EndpointSelector component using shadcn/ui Select components, connected to Zustand store.

    1. Create `frontend/src/components/console/endpoint-selector.tsx`:
       - Use Select, SelectTrigger, SelectValue, SelectContent, SelectItem from @/components/ui/select
       - Use useStore to access endpoint, mode, setEndpoint, setMode
       - Render two selects in a grid layout (2 columns on sm+)
       - Disable mode select when endpoint === 'chat-execute'
       - Use cn() for styling, follow existing component patterns

    2. Create `frontend/tests/components/console/endpoint-selector.test.tsx`:
       - Test: renders both selects
       - Test: endpoint select has correct options
       - Test: mode select has correct options
       - Test: selecting endpoint updates store
       - Test: selecting mode updates store
       - Test: mode select is disabled when endpoint is chat-execute

    Note: Wrap component in AppStoreProvider for tests. Use render from @testing-library/react.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/components/console/endpoint-selector.test.tsx</automated>
  </verify>
  <done>EndpointSelector component renders and controls endpoint/mode selection via Zustand store</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create MessageInput component</name>
  <files>
    frontend/src/components/console/message-input.tsx
    frontend/tests/components/console/message-input.test.tsx
  </files>
  <behavior>
    - MessageInput renders a Textarea with label
    - Textarea value is controlled by Zustand store message field
    - Typing in textarea updates the store
    - Optional: renders conversationId and traceId inputs (secondary fields)
  </behavior>
  <action>
    Create the MessageInput component using shadcn/ui Textarea, connected to Zustand store.

    1. Create `frontend/src/components/console/message-input.tsx`:
       - Use Textarea from @/components/ui/textarea
       - Use Input from @/components/ui/input
       - Use useStore to access message, conversationId, traceId and their setters
       - Render main message textarea (4 rows)
       - Render optional conversationId and traceId inputs in a grid
       - Add appropriate labels and placeholders

    2. Create `frontend/tests/components/console/message-input.test.tsx`:
       - Test: renders message textarea
       - Test: typing updates store
       - Test: renders conversationId input
       - Test: renders traceId input
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/components/console/message-input.test.tsx</automated>
  </verify>
  <done>MessageInput component renders and controls message/conversationId/traceId via Zustand store</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create ModelJsonPanel component</name>
  <files>
    frontend/src/components/console/model-json-panel.tsx
    frontend/tests/components/console/model-json-panel.test.tsx
  </files>
  <behavior>
    - ModelJsonPanel renders checkbox to include model
    - When checkbox is checked, shows collapsible panel with JSON textarea
    - Collapsible can be expanded/collapsed
    - JSON textarea uses monospace font
    - Invalid JSON shows error message (but doesn't prevent typing)
    - Panel shows modelFormat hint text
  </behavior>
  <action>
    Create the ModelJsonPanel component using Collapsible and Textarea, with JSON validation.

    1. Create `frontend/src/components/console/model-json-panel.tsx`:
       - Use Collapsible, CollapsibleTrigger, CollapsibleContent from @/components/ui/collapsible
       - Use Checkbox from @/components/ui/checkbox
       - Use Textarea from @/components/ui/textarea
       - Use Button from @/components/ui/button
       - Use useStore to access modelText, includeModel and their setters
       - Render checkbox with label "Include Model JSON"
       - When includeModel is true, show collapsible with expand/collapse button
       - Inside collapsible: Textarea with font-mono styling
       - Add JSON validation on change (show error inline, don't prevent typing)
       - Show hint: "modelFormat: structuremodel-v1"

    2. Create `frontend/tests/components/console/model-json-panel.test.tsx`:
       - Test: renders checkbox
       - Test: checking checkbox shows collapsible content
       - Test: collapsible expands/collapses
       - Test: textarea uses monospace font
       - Test: invalid JSON shows error message
       - Test: typing updates store

    Use the DEMO_MODEL_TEXT from existing AgentConsole as placeholder text.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/components/console/model-json-panel.test.tsx</automated>
  </verify>
  <done>ModelJsonPanel renders collapsible JSON input with validation</done>
</task>

<task type="auto">
  <name>Task 5: Create console barrel export</name>
  <files>frontend/src/components/console/index.ts</files>
  <action>
    Create barrel export file for console components.

    Create `frontend/src/components/console/index.ts`:
    ```typescript
    export { EndpointSelector } from './endpoint-selector'
    export { MessageInput } from './message-input'
    export { ModelJsonPanel } from './model-json-panel'
    ```
  </action>
  <verify>
    <automated>test -f frontend/src/components/console/index.ts</automated>
  </verify>
  <done>Barrel export exists for console components</done>
</task>

</tasks>

<verification>
- Extended console slice with form and execution state
- API contract types defined
- EndpointSelector component with tests
- MessageInput component with tests
- ModelJsonPanel component with tests
- All tests pass: `cd frontend && npm test -- --run`
</verification>

<success_criteria>
- CONS-01: User can select endpoint (agent-run, chat-message, chat-execute)
- CONS-02: User can select mode (chat, execute, auto)
- CONS-03: User can input message text
- CONS-04: User can optionally expand JSON model input area
- All components connected to Zustand store
- TDD workflow followed (tests written first)
</success_criteria>

<output>
After completion, create `.planning/phases/05-console-feature/05-01-SUMMARY.md`
</output>
