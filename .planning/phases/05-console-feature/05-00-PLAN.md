---
phase: 05-console-feature
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/tests/components/console/.gitkeep
  - frontend/tests/hooks/use-console-execution.test.ts
  - frontend/src/components/ui/checkbox.tsx
  - frontend/src/components/ui/collapsible.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Test infrastructure exists for console components"
    - "shadcn/ui Checkbox component is available"
    - "shadcn/ui Collapsible component is available"
  artifacts:
    - path: "frontend/tests/components/console/"
      provides: "Test directory for console component tests"
    - path: "frontend/src/components/ui/checkbox.tsx"
      provides: "Checkbox component for configuration options"
      exports: ["Checkbox"]
    - path: "frontend/src/components/ui/collapsible.tsx"
      provides: "Collapsible component for model JSON panel"
      exports: ["Collapsible", "CollapsibleTrigger", "CollapsibleContent"]
  key_links: []
---

<objective>
Set up test infrastructure and add missing shadcn/ui components required by console feature plans.

Purpose: Enable TDD workflow for console components and provide UI primitives for checkboxes and collapsible panels.
Output: Test stubs, Checkbox component, Collapsible component
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-console-feature/05-RESEARCH.md
@.planning/phases/05-console-feature/05-VALIDATION.md

## Existing Patterns

From frontend/tests/stores/slices/console.test.ts:
```typescript
import { describe, it, expect } from 'vitest'
import { createConsoleSlice, initialConsoleState, type ConsoleSlice } from '@/lib/stores/slices/console'
import { createStore } from 'zustand/vanilla'

describe('Console Slice (STAT-01)', () => {
  const createTestStore = () => {
    return createStore<ConsoleSlice>()((...args) => ({
      ...createConsoleSlice(...args),
    }))
  }
  // tests...
})
```

From frontend/tests/components/select.test.tsx:
```typescript
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
```

From frontend/vitest.config.ts:
```typescript
test: {
  environment: 'jsdom',
  globals: true,
  include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
  setupFiles: ['./tests/setup.ts'],
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create console test directory with test stubs</name>
  <files>
    frontend/tests/components/console/.gitkeep
    frontend/tests/hooks/use-console-execution.test.ts
  </files>
  <action>
    Create the console component test directory and a test stub file for the console execution hook.

    1. Create directory: `frontend/tests/components/console/`
    2. Add `.gitkeep` file to preserve the directory
    3. Create `frontend/tests/hooks/use-console-execution.test.ts` with it.todo() stubs:

    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('useConsoleExecution hook', () => {
      it.todo('executeSync should send POST request with correct payload')
      it.todo('executeSync should validate model JSON before sending')
      it.todo('executeSync should handle API errors')
      it.todo('executeStream should handle SSE connection')
      it.todo('executeStream should parse stream frames')
      it.todo('executeStream should update connection state')
    })
    ```
  </action>
  <verify>
    <automated>test -d frontend/tests/components/console && test -f frontend/tests/hooks/use-console-execution.test.ts</automated>
  </verify>
  <done>Console test directory exists, useConsoleExecution test stub created</done>
</task>

<task type="auto">
  <name>Task 2: Add shadcn/ui Checkbox component</name>
  <files>frontend/src/components/ui/checkbox.tsx</files>
  <action>
    Add the shadcn/ui Checkbox component using the standard CLI:

    ```bash
    cd frontend && npx shadcn@latest add checkbox
    ```

    This will:
    - Install @radix-ui/react-checkbox if not present
    - Create frontend/src/components/ui/checkbox.tsx with the component

    If the CLI fails, manually create the component following the existing shadcn/ui patterns in the project (see button.tsx, select.tsx for reference).
  </action>
  <verify>
    <automated>test -f frontend/src/components/ui/checkbox.tsx && grep -q "Checkbox" frontend/src/components/ui/checkbox.tsx</automated>
  </verify>
  <done>Checkbox component exists and exports Checkbox component</done>
</task>

<task type="auto">
  <name>Task 3: Add shadcn/ui Collapsible component</name>
  <files>frontend/src/components/ui/collapsible.tsx</files>
  <action>
    Add the shadcn/ui Collapsible component using the standard CLI:

    ```bash
    cd frontend && npx shadcn@latest add collapsible
    ```

    This will:
    - Install @radix-ui/react-collapsible if not present
    - Create frontend/src/components/ui/collapsible.tsx with the component

    If the CLI fails, manually create the component following the existing shadcn/ui patterns in the project.
  </action>
  <verify>
    <automated>test -f frontend/src/components/ui/collapsible.tsx && grep -q "Collapsible" frontend/src/components/ui/collapsible.tsx</automated>
  </verify>
  <done>Collapsible component exists and exports Collapsible, CollapsibleTrigger, CollapsibleContent</done>
</task>

</tasks>

<verification>
- Console test directory exists with .gitkeep
- useConsoleExecution test stub file exists
- Checkbox component exists at frontend/src/components/ui/checkbox.tsx
- Collapsible component exists at frontend/src/components/ui/collapsible.tsx
</verification>

<success_criteria>
- All test infrastructure in place for Wave 1+ plans
- Both shadcn/ui components available for console feature implementation
</success_criteria>

<output>
After completion, create `.planning/phases/05-console-feature/05-00-SUMMARY.md`
</output>
